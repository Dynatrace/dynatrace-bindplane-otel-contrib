type: restapi

status:
  distributions: [dbdot]
  codeowners:
    active: [dynatrace/TODO-DBDOT-CODEOWNERS-TEAM]
  class: receiver
  stability:
    beta: [logs, metrics]

tests:
  config:
    url: "https://api.example.com/data"
    auth_mode: "apikey"
    apikey:
      header_name: "X-API-Key"
      value: "test-key"
    metrics:
      name_field: "name"

